# medium-scrapper
## Setps:
1. npm install
2. node node src/services/scrap.js

This will return array of object. Object will contain title and link for medium blog. Default tag in file is 'java', you can change the tag variable data to extract other tag blogs.
